<Window x:Class="MEPGadgets.Scheme.View.SchemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MEPGadgets.Scheme" 
            xmlns:model="clr-namespace:MEPGadgets.Scheme.Model"
            mc:Ignorable="d" x:Name="Form"
            Width="800" Height="400"
            d:DesignHeight="250" d:DesignWidth="300" >
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFD8E5F9" Offset="0.019"/>
            <GradientStop Color="#FFE6D8F1" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
       
        <TreeView x:Name="treeView"
                  SelectedItemChanged="TreeView_SelectedItemChanged" 
                  MouseDoubleClick="TreeView_MouseDoubleClick"
                  Background="Transparent">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type model:SchemeBranch}" 
                                          ItemsSource="{Binding Items}" >
                    <Label Content="{Binding Name}"  />
                </HierarchicalDataTemplate>
               
                <DataTemplate DataType="{x:Type model:SchemeElement}">
                    <Border CornerRadius="3" BorderThickness="1">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="#FF775594"  />
                        </Border.BorderBrush>
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{Binding Name}" Grid.Column="0"/>
                            <Label Content="{Binding Category}" Grid.Column="1"/>
                            <Label Content="{Binding Size}" Grid.Column="2"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </TreeView.Resources>
            
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected}" />
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded}" />
                    <EventSetter Event="Selected" Handler="TreeViewSelectedItemChanged" />
                </Style>
            </TreeView.ItemContainerStyle>

        </TreeView>
        <GridSplitter  VerticalAlignment="Stretch" Width="5"/>
        <ListBox x:Name="LBox" Grid.Column="1" Background="Transparent" 
                 SelectionChanged="LBox_SelectionChanged">
            
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type model:SchemeElement}">
                    <Label Content="{Binding  Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
